{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": [],
    "typeRoots": [
      // add path to @types
      "node_modules/@types"
   ]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
